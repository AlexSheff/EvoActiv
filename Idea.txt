NeuroFormula — AI discovering new mathematical activation laws.

📘 Краткое описание (для GitHub)

NeuroFormula — экспериментальная система эволюционного поиска архитектур и нелинейных активаций для нейросетей.
Она комбинирует генетический алгоритм с символическим выражением функций, создавая и тестируя новые формулы на реальных данных.
Результаты показывают, что некоторые найденные функции (например, sin(2*x + cos(x))) превосходят классические активации при минимальной сложности.

Идея проекта — кратко (elevator pitch)

EvoFormNet — открытая исследовательская платформа для автоматического поиска и валидации новых математических формул и архитектур нейросетей.
Платформа генерирует формулы (включая активации, блоки, трансформации), проверяет их символьно и численно, интегрирует как модули в NN, бенчмаркет на задачах (MNIST, CIFAR, audio, PDE), ранжирует по многокритериальному скору и сохраняет артефакты. Цель — находить практичные, аппаратно-дружественные новшества (новые активации, микроблоки, инварианты), пригодные как для исследований, так и для внедрения.

Почему это имеет смысл (ценностное обоснование)

Возможность находить компактные, более устойчивые активации/микроблоки, дающие лучший trade-off accuracy/params/latency.

Слияние Symbolic Regression + NAS — новый класс AutoML, не ограниченный дискретными операциями.

Потенциальный научный вклад: открытие новых тождеств/инвариантов и публикации.

Сообщество и open-source: репозиторий + данные + артефакты = ресурc для исследований и практики.

Целевые пользователи

Исследователи ML (NAS, активации, физические модели).

ML-инженеры и стартапы, ищущие компактные, эффективные блоки.

Сообщество open-source, интересующееся symbolic discovery.

Что проект даст (KPIs и ROI)

Короткий ROI: библиотека, воспроизводимый pipeline, первые кандидаты-активации (MVP за 2–4 недели).

Средний ROI: набор из 5–20 проверенных новых функций/блоков с бенчмарками (3–6 мес).

Долгий ROI: публикации, интеграция в фреймворки, возможный коммерческий продукт (компактные модели, ускорители).

Критерии успеха (через 3 месяца):

MVP pipeline (grammar → proposer → verifier → bench) в GitHub.

10–50 протестированных кандидатов с артефактами.

1–3 кандидата с улучшением accuracy/params/latency на реальных задачах ≥ 0.5% или значимым сокращением параметров.

Ограничения и риски

Compute cost: масштабные поиски требуют GPU/кластера.

Ложноположительные открытия — требуется формальная/широкая валидация.

Аппаратная непригодность формул (непараллельные операции) — нужно penalize.

Интерпретируемость и adoption: сообщество осторожно относится к «магическим» формулам.

Ключевые гипотезы (что хотим проверить)

Автоматический поиск формул может найти активации, улучшающие accuracy/robustness при меньших параметрах.

Комбинация символьной верификации + численной проверки отсекает львиную долю лженаходок.

Proposer + RL/эволюция быстрее и эффективнее brute force.

Минимальный набор функций (MVP)

Grammar generator (BNF JSON) — синтаксис формул.

Random proposer + simple evolutionary loop (mutation, selection).

Verifier:

SymPy упрощение/дифференцирование.

Численная оценка на наборе точек (градиент, bounds).

Bench:

Auto-wrap top-K формул в PyTorch/TensorFlow активации; тест на MNIST (MLP/CNN).

Scoring: многокритериальный скор (accuracy, params, time, grad_friendly, flop_penalty).

Artifact store: JSONL, archive.csv, графики, saved models (top-k).

README + quickstart notebook.

Архитектура системы (высокоуровневая)

Компоненты:

Generator (G) — grammar.json + gen.py

Proposer (P) — random / evolutionary / transformer proposer

Verifier (V) — sympy + numeric tests (NumPy / JAX / PyTorch)

Bench (E) — PyTorch/TensorFlow wrappers, training harness

Scorer & Archivist — compute Score(F), save JSONL/CSV, visual reports

UI / Dashboard (optional) — Streamlit or simple notebook for human-in-loop

Storage: local artifacts dir or S3 (for scale).

Orchestration: scripts → later add Airflow/K8s for scale.

Технологический стек (предложение)

Ядро: Python 3.10+

Библиотеки: SymPy, NumPy, Pandas, PyTorch (рекомендую PyTorch для гибкости), optionally TensorFlow, scikit-learn, matplotlib.

Proposer: simple evolutionary (DEAP optional), later Transformers (HuggingFace).

Logging/Artifact: JSONL, CSV, MLflow or W&B (опционально).

CI: GitHub Actions, Dockerfile.

License: Apache-2.0 (научно-инженерный проект, разрешает коммерческое использование).

Репозиторий — структура (предложение)
evoformnet/
├─ README.md
├─ LICENSE
├─ pyproject.toml / requirements.txt
├─ docs/
│  └─ design.md
├─ evoformnet/
│  ├─ __init__.py
│  ├─ generator.py        # grammar, BNF loader
│  ├─ proposer.py         # random/evo proposer
│  ├─ verifier.py         # sympy numeric checks
│  ├─ bench/
│  │  ├─ trainer.py       # harness to train model with given activation
│  │  └─ models.py        # model templates
│  ├─ scorer.py
│  ├─ arch_search.py      # orchestration pipeline
│  └─ utils.py
├─ experiments/
│  └─ mnist_quickstart.ipynb
├─ examples/
│  └─ run_local.sh
└─ .github/
   └─ workflows/ci.yml

Техническое задание (кратко, чтобы перейти к делу)

Цель: реализовать MVP pipeline, способный автоматически генерировать формулы активаций, проверять и тестировать их на MNIST.

Требования:

generator.py — читать grammar.json, генерировать N формул, гарантировать безопасный subset (no pow var-var, safe division).

verifier.py — для каждой формулы:

символьное упрощение (SymPy),

проверить определённость на домене (no log ≤0, denom ≠ 0),

вычислить производную, численно оценить min/max and max|grad| on grid,

пометить grad_friendly boolean.

bench/trainer.py — принять функция активации (callable), встроить в MLP/CNN, обучить N_epochs, вернуть metrics (acc, loss, params, time).

proposer.py — evolutionary loop: mutate genomes, evaluate, select top-k, repeat.

scorer.py — реализовать Score = w1acc - w2params_norm - w3time + w4stability + penalty_if_not_grad_friendly.

arch_search.py — CLI: population, generations, epochs, batch, out_dir; сохраняет JSONL and archive.csv.

Тесты: unit tests для generator/simplifier/TF wrapper.

Документация: quickstart, examples, contribution guide.

MVP-дорожная карта (фазы и время)

Phase 0 — подготовка (1–2 дня)

Инициализация репо, шаблон, requirements, Dockerfile, README initial.

Phase 1 — ядро MVP (4–7 дней)

generator.py, verifier.py (SymPy checks), bench/trainer.py (PyTorch minimal), scorer + arch_search CLI.

Run quick experiments (MNIST) — produce first candidates.

Phase 2 — улучшения (2–4 недели)

proposer: evolutionary improvements, caching, archive.

TF conversion robustness, grad_friendly detection.

plot generation, JSONL provenance, top-K save.

Phase 3 — scale & usability (1–3 месяцев)

parallel eval (multiprocessing/k8s), W&B integration, Streamlit dashboard, extended grammar, advanced proposer (RL, transformer), community onboarding.

Phase 4 — research & publications (по результатам)

validate best candidates on multiple datasets, write paper/blog, release package on PyPI.

Первые конкретные шаги (actionable)

Создать GitHub репозиторий evoformnet (public), добавить LICENSE (Apache-2.0) и README skeleton.

Закоммитить minimal requirements.txt (sympy, numpy, pandas, torch, matplotlib).

Реализовать generator.py + verifier.py (внутри repo) — цель: за 1 день иметь runnable script python -m evoformnet.generator --n 100.

Реализовать bench/trainer.py (PyTorch MLP + wrapper для custom activation).

Собирать результаты в experiments/mnist_quickstart.ipynb.

Собрать first demo: запустить pipeline на 1000 кандидатов, получить top-10, записать в archive.csv.

Риски по реализации и mitigations

Комбинаторный взрыв → mitigation: грамматика с cost, proposer guided (learned), early filter by verifier.

Неустойчивые формулы → mitigation: grad_friendly flag и penalize, расширенные numeric tests.

High compute → mitigation: cheap proxies (few epochs, small datasets) + later full eval on top candidates.

Формат репозитория и первая структура задач (issues)

Issue #1: Init repo + basic README

Issue #2: Implement grammar.json + generator.py (safe grammar)

Issue #3: Implement verifier.py (SymPy checks + numeric grid)

Issue #4: Implement bench/trainer.py (PyTorch wrapper for activation)

Issue #5: Implement scorer & arch_search orchestration

Issue #6: Quick MNIST run + produce archive.csv